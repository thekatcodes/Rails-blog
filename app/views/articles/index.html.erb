<h1>Articles</h1>

<ul>
<%# Reference/access instance variable @articles to access data and replace html content with it  %>
<%# use ERB tags <%  and <%=  to evaluate enclosed ruby code (similar to EJS) %>
  <% @articles.each do |article| %>
    <li>
    <%# resources method sets up URL and path helpers, _path suffix  returns "/articles/#{article.id}" when given an article %>
      <a href="<%= article_path(article) %>">
        <%= article.title %>
      </a>    
    </li>
  <% end %>
</ul>